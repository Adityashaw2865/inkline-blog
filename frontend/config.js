// ============================================================
// config.js — single source of truth for the backend API URL.
// Change this one line when the backend URL changes (e.g. after
// deploying), instead of updating every fetch() call individually.
// ============================================================
export const API_URL = 'https://inkline-blog-backend.onrender.com';
